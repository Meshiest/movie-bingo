extend includes/layout.pug

block content
  if movies.length === 0
    +notice()
      span No movies found. Please 
      a(href='/movies/new') add
      span  one.
  else
    +card()
      header Movies
      ul
        for movie in movies
          li
            a(href=`/movies/${movie.id}`)= movie.title
